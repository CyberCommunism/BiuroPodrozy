// tslint:disable-next-line:class-name
export interface tripObj {
  name: string;
  aim: string;
  startTrip: string;
  endTrip: string;
  price: number;
  currency: string;
  maxSpace: number;
  description: string;
  imgURL: string;
  rate: Array<number>;
}
